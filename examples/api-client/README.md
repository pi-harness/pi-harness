# API client example

This example shows the smallest Node client for a running Pi Harness web server.

```sh
node client.mjs
```

Set `PI_HARNESS_URL` to use another server URL. The endpoint is intentionally read-only and can be used as a health check from scripts or desktop wrappers.
