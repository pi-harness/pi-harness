# Plugin starter

Copy this directory into `packages/plugins/` to start a built-in plugin. The plugin registers a small tool through the public plugin API and keeps all application-specific code out of the host.
