# Plugin development

Start with the [plugin authoring guide](../../README.md#author-a-plugin), the [plugin starter example](../../examples/plugin-starter), and the [community catalog rules](community.md). Built-in plugins live under `packages/plugins`; external plugins should remain independently installable and versioned.
