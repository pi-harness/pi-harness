{
  "name": "@pi-harness/desktop",
  "private": true,
  "version": "0.1.102",
  "type": "module",
  "scripts": {
    "build": "npm run build -w @pi-harness/web && npx tauri build",
    "dev": "npm run build -w @pi-harness/web && npx tauri dev",
    "bundle": "npm run build",
    "bundle:macos": "npm run build -w @pi-harness/web && npx tauri build --bundles app,dmg",
    "bundle:check": "npx tauri info"
  }
}
